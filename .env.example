# Environment (documentation only)

# Runtime reads os.environ. This file is not auto-loaded.
# Never put a real API key in the repository.

# fake (default) or openai
TAX_INTAKE_PROVIDER=fake

# Required only when TAX_INTAKE_PROVIDER=openai
OPENAI_API_KEY=
